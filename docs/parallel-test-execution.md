# Parallel test execution

The canonical command is `python tools/run_tests.py`. It loads `tools/parallel_test_policy.json`, composes its excluded markers with any `pytest.ini` `addopts` marker expression, emits concise output, and returns pytest's nonzero status on any worker failure. Serial and `--parallel` modes use the same exclusions; `--parallel` is only a reviewed local evidence run and requires `pytest-xdist`.

The current serial evidence is blocked at branch head `c59127a43d227bb14364a6b03b0a824079928814`. Exact command: `C:\G\python.exe tools/run_tests.py --junitxml tmp/qa-current.xml`. The bounded run started at 18:41:47Z and was hard-stopped at 18:43:47Z after the 120-second limit, with exit status `124`. Pytest had collected 181 tests and was still executing when interrupted; no completed JUnit report was flushed (`tmp/qa-current.xml` is absent), so there are no valid completed pass/fail/error counts to report. This is an explicitly blocked, non-comparable baseline and is not evidence that parallel execution is safe. Next validation is to rerun the exact command to completion and preserve its JUnit counts and exit status. The `ci_long_running` exclusion remains explicit in CI, simulator/process resource sharing has not passed repeated isolation testing, and `FR-20260826-workspace-ci-exclusion-audit` is a rebase/conflict dependency for changing the exclusion policy.

The current bounded serial attempt is blocked at branch head `c59127a43d227bb14364a6b03b0a824079928814`. Exact command: `C:\G\python.exe tools/run_tests.py --junitxml tmp/qa-current-bounded.xml`. It was started on 2026-08-29 UTC and interrupted after 120 seconds with exit status `124`; 181 tests were collected and execution was still active, so no completed current counts are claimed. The diagnostic artifact `tmp/qa-rerun.xml` was removed before final architecture review and is absent from the intended diff. This is an explicitly blocked, non-comparable baseline and is not evidence that parallel execution is safe. Next validation is to rerun the exact command to completion and preserve its JUnit counts and exit status. The `ci_long_running` exclusion remains explicit in CI, simulator/process resource sharing has not passed repeated isolation testing, and `FR-20260826-workspace-ci-exclusion-audit` is a rebase/conflict dependency for changing the exclusion policy.

Serial fallback: `C:\G\python.exe tools/run_tests.py --junitxml tmp/serial.xml`.