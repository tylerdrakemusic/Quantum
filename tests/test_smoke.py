# PLACEHOLDER — created by FR-20260425-ci-test-harness-gateway
# REMOVE this file once real tests exist for this project.
# Purpose: gives pytest something to assert so the CI workflow exits 0
# instead of "no tests collected" on a fresh project.
"""Smoke test — replace with real tests."""


def test_smoke_imports():
    # PLACEHOLDER — replace with real import sanity check or remove entirely
    assert True
